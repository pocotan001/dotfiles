``` sh
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
brew install python git
sudo easy_install pip
pip install cider
git clone --recursive git@github.com:pocotan001/dotfiles.git ~/.cider
cider restore
```
